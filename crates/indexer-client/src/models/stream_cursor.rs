/*
 * Untron Indexed Data API
 *
 * Read-only HTTP API served by PostgREST.  This schema contains only views. Each view exposes either: - canonical raw event data (`api.event_appended`), or - \"current state\" derived from events (views over `hub.*` and `ctl.*` versioned tables), or - append-only ledgers for actions (views over `hub.*_ledger` and `ctl.*_ledger` tables).  The underlying protocol is Untron V3: - hub (EVM): `UntronV3Index` emits a hash-chained event stream - controller (Tron): `UntronControllerIndex` emits a hash-chained event stream  All state shown here is derived deterministically from those streams and is reorg-safe.
 *
 * The version of the OpenAPI document: 14.2
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// StreamCursor : Projection cursors (per stream)  Shows how far the database has applied canonical events to derived state tables.  Fields: - `applied_through_seq`: highest contiguous canonical event_seq applied - `tip`: expected prev_tip for the next event - `updated_at`: last time the cursor advanced/rolled back
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct StreamCursor {
    /// Stream name (`hub` or `controller`)  Note: This is a Primary Key.<pk/>
    #[serde(rename = "stream", skip_serializing_if = "Option::is_none")]
    pub stream: Option<Stream>,
    /// Highest contiguous canonical event sequence already applied to derived tables
    #[serde(rename = "applied_through_seq", skip_serializing_if = "Option::is_none")]
    pub applied_through_seq: Option<i32>,
    /// Expected `prev_tip` for the next event to apply (hash-chain continuity check)
    #[serde(rename = "tip", skip_serializing_if = "Option::is_none")]
    pub tip: Option<String>,
    /// Timestamp when the cursor last advanced or rolled back
    #[serde(rename = "updated_at", skip_serializing_if = "Option::is_none")]
    pub updated_at: Option<String>,
}

impl StreamCursor {
    /// Projection cursors (per stream)  Shows how far the database has applied canonical events to derived state tables.  Fields: - `applied_through_seq`: highest contiguous canonical event_seq applied - `tip`: expected prev_tip for the next event - `updated_at`: last time the cursor advanced/rolled back
    pub fn new() -> StreamCursor {
        StreamCursor {
            stream: None,
            applied_through_seq: None,
            tip: None,
            updated_at: None,
        }
    }
}
/// Stream name (`hub` or `controller`)  Note: This is a Primary Key.<pk/>
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Stream {
    #[serde(rename = "hub")]
    Hub,
    #[serde(rename = "controller")]
    Controller,
}

impl Default for Stream {
    fn default() -> Stream {
        Self::Hub
    }
}

