FOUNDRY_PROFILE=production # or development

# Used by infra/docker-compose.yml to set the PostgREST login role password.
PGRST_AUTH_PASSWORD=

# Used by apps/indexer (sqlx migrator) and local tooling.
DATABASE_URL=postgres://postgres:postgres@localhost:5433/untron
