{
  "name": "@untron/v3-contracts",
  "version": "0.1.0",
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "sideEffects": false,
  "type": "module",
  "files": [
    "abi",
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist abi/generated.ts",
    "build": "forge build",
    "export": "pnpm run clean && pnpm run build && pnpm -w wagmi generate && tsc -p tsconfig.build.json"
  },
  "exports": {
    ".": {
      "types": "./dist/generated.d.ts",
      "default": "./dist/generated.js"
    },
    "./abi/*": "./abi/*"
  },
  "devDependencies": {
    "typescript": "^5",
    "rimraf": "^5"
  }
}
