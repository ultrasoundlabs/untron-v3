// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v5.29.3
// source: core/contract/account_contract.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { AccountType, accountTypeFromJSON, accountTypeToJSON, Permission } from "../Tron.js";

export const protobufPackage = "protocol";

export interface AccountCreateContract {
  ownerAddress: Buffer;
  accountAddress: Buffer;
  type: AccountType;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/** Update account name. Account name is not unique now. */
export interface AccountUpdateContract {
  accountName: Buffer;
  ownerAddress: Buffer;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

/** Set account id if the account has no id. Account id is unique and case insensitive. */
export interface SetAccountIdContract {
  accountId: Buffer;
  ownerAddress: Buffer;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

export interface AccountPermissionUpdateContract {
  ownerAddress: Buffer;
  /** Empty is invalidate */
  owner:
    | Permission
    | undefined;
  /** Can be empty */
  witness:
    | Permission
    | undefined;
  /** Empty is invalidate */
  actives: Permission[];
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBaseAccountCreateContract(): AccountCreateContract {
  return { ownerAddress: Buffer.alloc(0), accountAddress: Buffer.alloc(0), type: 0, _unknownFields: {} };
}

export const AccountCreateContract = {
  encode(message: AccountCreateContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (message.accountAddress.length !== 0) {
      writer.uint32(18).bytes(message.accountAddress);
    }
    if (message.type !== 0) {
      writer.uint32(24).int32(message.type);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag);
          (writer as any)["_push"](
            (val: Uint8Array, buf: Buffer, pos: number) => buf.set(val, pos),
            value.length,
            value,
          );
        }
      }
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AccountCreateContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccountCreateContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.accountAddress = reader.bytes() as Buffer;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const startPos = reader.pos;
      reader.skipType(tag & 7);
      const buf = reader.buf.slice(startPos, reader.pos);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): AccountCreateContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      accountAddress: isSet(object.accountAddress)
        ? Buffer.from(bytesFromBase64(object.accountAddress))
        : Buffer.alloc(0),
      type: isSet(object.type) ? accountTypeFromJSON(object.type) : 0,
    };
  },

  toJSON(message: AccountCreateContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (message.accountAddress.length !== 0) {
      obj.accountAddress = base64FromBytes(message.accountAddress);
    }
    if (message.type !== 0) {
      obj.type = accountTypeToJSON(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AccountCreateContract>, I>>(base?: I): AccountCreateContract {
    return AccountCreateContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AccountCreateContract>, I>>(object: I): AccountCreateContract {
    const message = createBaseAccountCreateContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.accountAddress = object.accountAddress ?? Buffer.alloc(0);
    message.type = object.type ?? 0;
    return message;
  },
};

function createBaseAccountUpdateContract(): AccountUpdateContract {
  return { accountName: Buffer.alloc(0), ownerAddress: Buffer.alloc(0), _unknownFields: {} };
}

export const AccountUpdateContract = {
  encode(message: AccountUpdateContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.accountName.length !== 0) {
      writer.uint32(10).bytes(message.accountName);
    }
    if (message.ownerAddress.length !== 0) {
      writer.uint32(18).bytes(message.ownerAddress);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag);
          (writer as any)["_push"](
            (val: Uint8Array, buf: Buffer, pos: number) => buf.set(val, pos),
            value.length,
            value,
          );
        }
      }
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AccountUpdateContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccountUpdateContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.accountName = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const startPos = reader.pos;
      reader.skipType(tag & 7);
      const buf = reader.buf.slice(startPos, reader.pos);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): AccountUpdateContract {
    return {
      accountName: isSet(object.accountName) ? Buffer.from(bytesFromBase64(object.accountName)) : Buffer.alloc(0),
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
    };
  },

  toJSON(message: AccountUpdateContract): unknown {
    const obj: any = {};
    if (message.accountName.length !== 0) {
      obj.accountName = base64FromBytes(message.accountName);
    }
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AccountUpdateContract>, I>>(base?: I): AccountUpdateContract {
    return AccountUpdateContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AccountUpdateContract>, I>>(object: I): AccountUpdateContract {
    const message = createBaseAccountUpdateContract();
    message.accountName = object.accountName ?? Buffer.alloc(0);
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseSetAccountIdContract(): SetAccountIdContract {
  return { accountId: Buffer.alloc(0), ownerAddress: Buffer.alloc(0), _unknownFields: {} };
}

export const SetAccountIdContract = {
  encode(message: SetAccountIdContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.accountId.length !== 0) {
      writer.uint32(10).bytes(message.accountId);
    }
    if (message.ownerAddress.length !== 0) {
      writer.uint32(18).bytes(message.ownerAddress);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag);
          (writer as any)["_push"](
            (val: Uint8Array, buf: Buffer, pos: number) => buf.set(val, pos),
            value.length,
            value,
          );
        }
      }
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SetAccountIdContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetAccountIdContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.accountId = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const startPos = reader.pos;
      reader.skipType(tag & 7);
      const buf = reader.buf.slice(startPos, reader.pos);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): SetAccountIdContract {
    return {
      accountId: isSet(object.accountId) ? Buffer.from(bytesFromBase64(object.accountId)) : Buffer.alloc(0),
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
    };
  },

  toJSON(message: SetAccountIdContract): unknown {
    const obj: any = {};
    if (message.accountId.length !== 0) {
      obj.accountId = base64FromBytes(message.accountId);
    }
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetAccountIdContract>, I>>(base?: I): SetAccountIdContract {
    return SetAccountIdContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetAccountIdContract>, I>>(object: I): SetAccountIdContract {
    const message = createBaseSetAccountIdContract();
    message.accountId = object.accountId ?? Buffer.alloc(0);
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseAccountPermissionUpdateContract(): AccountPermissionUpdateContract {
  return { ownerAddress: Buffer.alloc(0), owner: undefined, witness: undefined, actives: [], _unknownFields: {} };
}

export const AccountPermissionUpdateContract = {
  encode(message: AccountPermissionUpdateContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (message.owner !== undefined) {
      Permission.encode(message.owner, writer.uint32(18).fork()).ldelim();
    }
    if (message.witness !== undefined) {
      Permission.encode(message.witness, writer.uint32(26).fork()).ldelim();
    }
    for (const v of message.actives) {
      Permission.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag);
          (writer as any)["_push"](
            (val: Uint8Array, buf: Buffer, pos: number) => buf.set(val, pos),
            value.length,
            value,
          );
        }
      }
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AccountPermissionUpdateContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccountPermissionUpdateContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.owner = Permission.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.witness = Permission.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.actives.push(Permission.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const startPos = reader.pos;
      reader.skipType(tag & 7);
      const buf = reader.buf.slice(startPos, reader.pos);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): AccountPermissionUpdateContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      owner: isSet(object.owner) ? Permission.fromJSON(object.owner) : undefined,
      witness: isSet(object.witness) ? Permission.fromJSON(object.witness) : undefined,
      actives: globalThis.Array.isArray(object?.actives) ? object.actives.map((e: any) => Permission.fromJSON(e)) : [],
    };
  },

  toJSON(message: AccountPermissionUpdateContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (message.owner !== undefined) {
      obj.owner = Permission.toJSON(message.owner);
    }
    if (message.witness !== undefined) {
      obj.witness = Permission.toJSON(message.witness);
    }
    if (message.actives?.length) {
      obj.actives = message.actives.map((e) => Permission.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AccountPermissionUpdateContract>, I>>(base?: I): AccountPermissionUpdateContract {
    return AccountPermissionUpdateContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AccountPermissionUpdateContract>, I>>(
    object: I,
  ): AccountPermissionUpdateContract {
    const message = createBaseAccountPermissionUpdateContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.owner = (object.owner !== undefined && object.owner !== null)
      ? Permission.fromPartial(object.owner)
      : undefined;
    message.witness = (object.witness !== undefined && object.witness !== null)
      ? Permission.fromPartial(object.witness)
      : undefined;
    message.actives = object.actives?.map((e) => Permission.fromPartial(e)) || [];
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
