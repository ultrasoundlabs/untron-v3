// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v5.29.3
// source: core/contract/proposal_contract.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "protocol";

export interface ProposalApproveContract {
  ownerAddress: Buffer;
  proposalId: Long;
  /** add or remove approval */
  isAddApproval: boolean;
}

export interface ProposalCreateContract {
  ownerAddress: Buffer;
  parameters: Map<Long, Long>;
}

export interface ProposalCreateContract_ParametersEntry {
  key: Long;
  value: Long;
}

export interface ProposalDeleteContract {
  ownerAddress: Buffer;
  proposalId: Long;
}

function createBaseProposalApproveContract(): ProposalApproveContract {
  return { ownerAddress: Buffer.alloc(0), proposalId: Long.ZERO, isAddApproval: false };
}

export const ProposalApproveContract = {
  encode(message: ProposalApproveContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (!message.proposalId.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.proposalId);
    }
    if (message.isAddApproval !== false) {
      writer.uint32(24).bool(message.isAddApproval);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ProposalApproveContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProposalApproveContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.proposalId = reader.int64() as Long;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.isAddApproval = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProposalApproveContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      proposalId: isSet(object.proposalId) ? Long.fromValue(object.proposalId) : Long.ZERO,
      isAddApproval: isSet(object.isAddApproval) ? globalThis.Boolean(object.isAddApproval) : false,
    };
  },

  toJSON(message: ProposalApproveContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (!message.proposalId.equals(Long.ZERO)) {
      obj.proposalId = (message.proposalId || Long.ZERO).toString();
    }
    if (message.isAddApproval !== false) {
      obj.isAddApproval = message.isAddApproval;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProposalApproveContract>, I>>(base?: I): ProposalApproveContract {
    return ProposalApproveContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProposalApproveContract>, I>>(object: I): ProposalApproveContract {
    const message = createBaseProposalApproveContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.proposalId = (object.proposalId !== undefined && object.proposalId !== null)
      ? Long.fromValue(object.proposalId)
      : Long.ZERO;
    message.isAddApproval = object.isAddApproval ?? false;
    return message;
  },
};

function createBaseProposalCreateContract(): ProposalCreateContract {
  return { ownerAddress: Buffer.alloc(0), parameters: new Map() };
}

export const ProposalCreateContract = {
  encode(message: ProposalCreateContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    message.parameters.forEach((value, key) => {
      ProposalCreateContract_ParametersEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).ldelim();
    });
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ProposalCreateContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProposalCreateContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          const entry2 = ProposalCreateContract_ParametersEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.parameters.set(entry2.key, entry2.value);
          }
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProposalCreateContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      parameters: isObject(object.parameters)
        ? Object.entries(object.parameters).reduce<Map<Long, Long>>((acc, [key, value]) => {
          acc.set(Long.fromValue(key), Long.fromValue(value as Long | string));
          return acc;
        }, new Map())
        : new Map(),
    };
  },

  toJSON(message: ProposalCreateContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (message.parameters?.size) {
      obj.parameters = {};
      message.parameters.forEach((v, k) => {
        obj.parameters[longToNumber(k)] = v.toString();
      });
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProposalCreateContract>, I>>(base?: I): ProposalCreateContract {
    return ProposalCreateContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProposalCreateContract>, I>>(object: I): ProposalCreateContract {
    const message = createBaseProposalCreateContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.parameters = (() => {
      const m = new Map();
      (object.parameters as Map<Long, Long> ?? new Map()).forEach((value, key) => {
        if (value !== undefined) {
          m.set(key, Long.fromValue(value));
        }
      });
      return m;
    })();
    return message;
  },
};

function createBaseProposalCreateContract_ParametersEntry(): ProposalCreateContract_ParametersEntry {
  return { key: Long.ZERO, value: Long.ZERO };
}

export const ProposalCreateContract_ParametersEntry = {
  encode(message: ProposalCreateContract_ParametersEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (!message.key.equals(Long.ZERO)) {
      writer.uint32(8).int64(message.key);
    }
    if (!message.value.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ProposalCreateContract_ParametersEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProposalCreateContract_ParametersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.key = reader.int64() as Long;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.value = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProposalCreateContract_ParametersEntry {
    return {
      key: isSet(object.key) ? Long.fromValue(object.key) : Long.ZERO,
      value: isSet(object.value) ? Long.fromValue(object.value) : Long.ZERO,
    };
  },

  toJSON(message: ProposalCreateContract_ParametersEntry): unknown {
    const obj: any = {};
    if (!message.key.equals(Long.ZERO)) {
      obj.key = (message.key || Long.ZERO).toString();
    }
    if (!message.value.equals(Long.ZERO)) {
      obj.value = (message.value || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProposalCreateContract_ParametersEntry>, I>>(
    base?: I,
  ): ProposalCreateContract_ParametersEntry {
    return ProposalCreateContract_ParametersEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProposalCreateContract_ParametersEntry>, I>>(
    object: I,
  ): ProposalCreateContract_ParametersEntry {
    const message = createBaseProposalCreateContract_ParametersEntry();
    message.key = (object.key !== undefined && object.key !== null) ? Long.fromValue(object.key) : Long.ZERO;
    message.value = (object.value !== undefined && object.value !== null) ? Long.fromValue(object.value) : Long.ZERO;
    return message;
  },
};

function createBaseProposalDeleteContract(): ProposalDeleteContract {
  return { ownerAddress: Buffer.alloc(0), proposalId: Long.ZERO };
}

export const ProposalDeleteContract = {
  encode(message: ProposalDeleteContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (!message.proposalId.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.proposalId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ProposalDeleteContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProposalDeleteContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.proposalId = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProposalDeleteContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      proposalId: isSet(object.proposalId) ? Long.fromValue(object.proposalId) : Long.ZERO,
    };
  },

  toJSON(message: ProposalDeleteContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (!message.proposalId.equals(Long.ZERO)) {
      obj.proposalId = (message.proposalId || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProposalDeleteContract>, I>>(base?: I): ProposalDeleteContract {
    return ProposalDeleteContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProposalDeleteContract>, I>>(object: I): ProposalDeleteContract {
    const message = createBaseProposalDeleteContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.proposalId = (object.proposalId !== undefined && object.proposalId !== null)
      ? Long.fromValue(object.proposalId)
      : Long.ZERO;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
