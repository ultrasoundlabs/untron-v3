// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v5.29.3
// source: core/contract/witness_contract.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "protocol";

export interface WitnessCreateContract {
  ownerAddress: Buffer;
  url: Buffer;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

export interface WitnessUpdateContract {
  ownerAddress: Buffer;
  updateUrl: Buffer;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

export interface VoteWitnessContract {
  ownerAddress: Buffer;
  votes: VoteWitnessContract_Vote[];
  support: boolean;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

export interface VoteWitnessContract_Vote {
  voteAddress: Buffer;
  voteCount: Long;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBaseWitnessCreateContract(): WitnessCreateContract {
  return { ownerAddress: Buffer.alloc(0), url: Buffer.alloc(0), _unknownFields: {} };
}

export const WitnessCreateContract = {
  encode(message: WitnessCreateContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (message.url.length !== 0) {
      writer.uint32(18).bytes(message.url);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag);
          (writer as any)["_push"](
            (val: Uint8Array, buf: Buffer, pos: number) => buf.set(val, pos),
            value.length,
            value,
          );
        }
      }
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): WitnessCreateContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWitnessCreateContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.url = reader.bytes() as Buffer;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const startPos = reader.pos;
      reader.skipType(tag & 7);
      const buf = reader.buf.slice(startPos, reader.pos);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): WitnessCreateContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      url: isSet(object.url) ? Buffer.from(bytesFromBase64(object.url)) : Buffer.alloc(0),
    };
  },

  toJSON(message: WitnessCreateContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (message.url.length !== 0) {
      obj.url = base64FromBytes(message.url);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WitnessCreateContract>, I>>(base?: I): WitnessCreateContract {
    return WitnessCreateContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WitnessCreateContract>, I>>(object: I): WitnessCreateContract {
    const message = createBaseWitnessCreateContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.url = object.url ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseWitnessUpdateContract(): WitnessUpdateContract {
  return { ownerAddress: Buffer.alloc(0), updateUrl: Buffer.alloc(0), _unknownFields: {} };
}

export const WitnessUpdateContract = {
  encode(message: WitnessUpdateContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (message.updateUrl.length !== 0) {
      writer.uint32(98).bytes(message.updateUrl);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag);
          (writer as any)["_push"](
            (val: Uint8Array, buf: Buffer, pos: number) => buf.set(val, pos),
            value.length,
            value,
          );
        }
      }
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): WitnessUpdateContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWitnessUpdateContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.updateUrl = reader.bytes() as Buffer;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const startPos = reader.pos;
      reader.skipType(tag & 7);
      const buf = reader.buf.slice(startPos, reader.pos);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): WitnessUpdateContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      updateUrl: isSet(object.updateUrl) ? Buffer.from(bytesFromBase64(object.updateUrl)) : Buffer.alloc(0),
    };
  },

  toJSON(message: WitnessUpdateContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (message.updateUrl.length !== 0) {
      obj.updateUrl = base64FromBytes(message.updateUrl);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WitnessUpdateContract>, I>>(base?: I): WitnessUpdateContract {
    return WitnessUpdateContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WitnessUpdateContract>, I>>(object: I): WitnessUpdateContract {
    const message = createBaseWitnessUpdateContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.updateUrl = object.updateUrl ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseVoteWitnessContract(): VoteWitnessContract {
  return { ownerAddress: Buffer.alloc(0), votes: [], support: false, _unknownFields: {} };
}

export const VoteWitnessContract = {
  encode(message: VoteWitnessContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    for (const v of message.votes) {
      VoteWitnessContract_Vote.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.support !== false) {
      writer.uint32(24).bool(message.support);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag);
          (writer as any)["_push"](
            (val: Uint8Array, buf: Buffer, pos: number) => buf.set(val, pos),
            value.length,
            value,
          );
        }
      }
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): VoteWitnessContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoteWitnessContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.votes.push(VoteWitnessContract_Vote.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.support = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const startPos = reader.pos;
      reader.skipType(tag & 7);
      const buf = reader.buf.slice(startPos, reader.pos);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): VoteWitnessContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      votes: globalThis.Array.isArray(object?.votes)
        ? object.votes.map((e: any) => VoteWitnessContract_Vote.fromJSON(e))
        : [],
      support: isSet(object.support) ? globalThis.Boolean(object.support) : false,
    };
  },

  toJSON(message: VoteWitnessContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (message.votes?.length) {
      obj.votes = message.votes.map((e) => VoteWitnessContract_Vote.toJSON(e));
    }
    if (message.support !== false) {
      obj.support = message.support;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VoteWitnessContract>, I>>(base?: I): VoteWitnessContract {
    return VoteWitnessContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VoteWitnessContract>, I>>(object: I): VoteWitnessContract {
    const message = createBaseVoteWitnessContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.votes = object.votes?.map((e) => VoteWitnessContract_Vote.fromPartial(e)) || [];
    message.support = object.support ?? false;
    return message;
  },
};

function createBaseVoteWitnessContract_Vote(): VoteWitnessContract_Vote {
  return { voteAddress: Buffer.alloc(0), voteCount: Long.ZERO, _unknownFields: {} };
}

export const VoteWitnessContract_Vote = {
  encode(message: VoteWitnessContract_Vote, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.voteAddress.length !== 0) {
      writer.uint32(10).bytes(message.voteAddress);
    }
    if (!message.voteCount.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.voteCount);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag);
          (writer as any)["_push"](
            (val: Uint8Array, buf: Buffer, pos: number) => buf.set(val, pos),
            value.length,
            value,
          );
        }
      }
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): VoteWitnessContract_Vote {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoteWitnessContract_Vote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.voteAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.voteCount = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const startPos = reader.pos;
      reader.skipType(tag & 7);
      const buf = reader.buf.slice(startPos, reader.pos);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): VoteWitnessContract_Vote {
    return {
      voteAddress: isSet(object.voteAddress) ? Buffer.from(bytesFromBase64(object.voteAddress)) : Buffer.alloc(0),
      voteCount: isSet(object.voteCount) ? Long.fromValue(object.voteCount) : Long.ZERO,
    };
  },

  toJSON(message: VoteWitnessContract_Vote): unknown {
    const obj: any = {};
    if (message.voteAddress.length !== 0) {
      obj.voteAddress = base64FromBytes(message.voteAddress);
    }
    if (!message.voteCount.equals(Long.ZERO)) {
      obj.voteCount = (message.voteCount || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VoteWitnessContract_Vote>, I>>(base?: I): VoteWitnessContract_Vote {
    return VoteWitnessContract_Vote.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VoteWitnessContract_Vote>, I>>(object: I): VoteWitnessContract_Vote {
    const message = createBaseVoteWitnessContract_Vote();
    message.voteAddress = object.voteAddress ?? Buffer.alloc(0);
    message.voteCount = (object.voteCount !== undefined && object.voteCount !== null)
      ? Long.fromValue(object.voteCount)
      : Long.ZERO;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
