// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v5.29.3
// source: core/contract/market_contract.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "protocol";

export interface MarketSellAssetContract {
  ownerAddress: Buffer;
  sellTokenId: Buffer;
  sellTokenQuantity: Long;
  buyTokenId: Buffer;
  /** min to receive */
  buyTokenQuantity: Long;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

export interface MarketCancelOrderContract {
  ownerAddress: Buffer;
  orderId: Buffer;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBaseMarketSellAssetContract(): MarketSellAssetContract {
  return {
    ownerAddress: Buffer.alloc(0),
    sellTokenId: Buffer.alloc(0),
    sellTokenQuantity: Long.ZERO,
    buyTokenId: Buffer.alloc(0),
    buyTokenQuantity: Long.ZERO,
    _unknownFields: {},
  };
}

export const MarketSellAssetContract = {
  encode(message: MarketSellAssetContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (message.sellTokenId.length !== 0) {
      writer.uint32(18).bytes(message.sellTokenId);
    }
    if (!message.sellTokenQuantity.equals(Long.ZERO)) {
      writer.uint32(24).int64(message.sellTokenQuantity);
    }
    if (message.buyTokenId.length !== 0) {
      writer.uint32(34).bytes(message.buyTokenId);
    }
    if (!message.buyTokenQuantity.equals(Long.ZERO)) {
      writer.uint32(40).int64(message.buyTokenQuantity);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag);
          (writer as any)["_push"](
            (val: Uint8Array, buf: Buffer, pos: number) => buf.set(val, pos),
            value.length,
            value,
          );
        }
      }
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MarketSellAssetContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketSellAssetContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.sellTokenId = reader.bytes() as Buffer;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.sellTokenQuantity = reader.int64() as Long;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.buyTokenId = reader.bytes() as Buffer;
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.buyTokenQuantity = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const startPos = reader.pos;
      reader.skipType(tag & 7);
      const buf = reader.buf.slice(startPos, reader.pos);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): MarketSellAssetContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      sellTokenId: isSet(object.sellTokenId) ? Buffer.from(bytesFromBase64(object.sellTokenId)) : Buffer.alloc(0),
      sellTokenQuantity: isSet(object.sellTokenQuantity) ? Long.fromValue(object.sellTokenQuantity) : Long.ZERO,
      buyTokenId: isSet(object.buyTokenId) ? Buffer.from(bytesFromBase64(object.buyTokenId)) : Buffer.alloc(0),
      buyTokenQuantity: isSet(object.buyTokenQuantity) ? Long.fromValue(object.buyTokenQuantity) : Long.ZERO,
    };
  },

  toJSON(message: MarketSellAssetContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (message.sellTokenId.length !== 0) {
      obj.sellTokenId = base64FromBytes(message.sellTokenId);
    }
    if (!message.sellTokenQuantity.equals(Long.ZERO)) {
      obj.sellTokenQuantity = (message.sellTokenQuantity || Long.ZERO).toString();
    }
    if (message.buyTokenId.length !== 0) {
      obj.buyTokenId = base64FromBytes(message.buyTokenId);
    }
    if (!message.buyTokenQuantity.equals(Long.ZERO)) {
      obj.buyTokenQuantity = (message.buyTokenQuantity || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MarketSellAssetContract>, I>>(base?: I): MarketSellAssetContract {
    return MarketSellAssetContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarketSellAssetContract>, I>>(object: I): MarketSellAssetContract {
    const message = createBaseMarketSellAssetContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.sellTokenId = object.sellTokenId ?? Buffer.alloc(0);
    message.sellTokenQuantity = (object.sellTokenQuantity !== undefined && object.sellTokenQuantity !== null)
      ? Long.fromValue(object.sellTokenQuantity)
      : Long.ZERO;
    message.buyTokenId = object.buyTokenId ?? Buffer.alloc(0);
    message.buyTokenQuantity = (object.buyTokenQuantity !== undefined && object.buyTokenQuantity !== null)
      ? Long.fromValue(object.buyTokenQuantity)
      : Long.ZERO;
    return message;
  },
};

function createBaseMarketCancelOrderContract(): MarketCancelOrderContract {
  return { ownerAddress: Buffer.alloc(0), orderId: Buffer.alloc(0), _unknownFields: {} };
}

export const MarketCancelOrderContract = {
  encode(message: MarketCancelOrderContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (message.orderId.length !== 0) {
      writer.uint32(18).bytes(message.orderId);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag);
          (writer as any)["_push"](
            (val: Uint8Array, buf: Buffer, pos: number) => buf.set(val, pos),
            value.length,
            value,
          );
        }
      }
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MarketCancelOrderContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketCancelOrderContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.orderId = reader.bytes() as Buffer;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const startPos = reader.pos;
      reader.skipType(tag & 7);
      const buf = reader.buf.slice(startPos, reader.pos);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): MarketCancelOrderContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      orderId: isSet(object.orderId) ? Buffer.from(bytesFromBase64(object.orderId)) : Buffer.alloc(0),
    };
  },

  toJSON(message: MarketCancelOrderContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (message.orderId.length !== 0) {
      obj.orderId = base64FromBytes(message.orderId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MarketCancelOrderContract>, I>>(base?: I): MarketCancelOrderContract {
    return MarketCancelOrderContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarketCancelOrderContract>, I>>(object: I): MarketCancelOrderContract {
    const message = createBaseMarketCancelOrderContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.orderId = object.orderId ?? Buffer.alloc(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
