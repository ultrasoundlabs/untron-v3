// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v5.29.3
// source: core/contract/balance_contract.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { ResourceCode, resourceCodeFromJSON, resourceCodeToJSON } from "./common.js";

export const protobufPackage = "protocol";

export interface FreezeBalanceContract {
  ownerAddress: Buffer;
  frozenBalance: Long;
  frozenDuration: Long;
  resource: ResourceCode;
  receiverAddress: Buffer;
}

export interface UnfreezeBalanceContract {
  ownerAddress: Buffer;
  resource: ResourceCode;
  receiverAddress: Buffer;
}

export interface WithdrawBalanceContract {
  ownerAddress: Buffer;
}

export interface TransferContract {
  ownerAddress: Buffer;
  toAddress: Buffer;
  amount: Long;
}

export interface TransactionBalanceTrace {
  transactionIdentifier: Buffer;
  operation: TransactionBalanceTrace_Operation[];
  type: string;
  status: string;
}

export interface TransactionBalanceTrace_Operation {
  operationIdentifier: Long;
  address: Buffer;
  amount: Long;
}

export interface BlockBalanceTrace {
  blockIdentifier: BlockBalanceTrace_BlockIdentifier | undefined;
  timestamp: Long;
  /** BlockIdentifier parent_block_identifier = 4; */
  transactionBalanceTrace: TransactionBalanceTrace[];
}

export interface BlockBalanceTrace_BlockIdentifier {
  hash: Buffer;
  number: Long;
}

export interface AccountTrace {
  balance: Long;
  placeholder: Long;
}

export interface AccountIdentifier {
  address: Buffer;
}

export interface AccountBalanceRequest {
  accountIdentifier: AccountIdentifier | undefined;
  blockIdentifier: BlockBalanceTrace_BlockIdentifier | undefined;
}

export interface AccountBalanceResponse {
  balance: Long;
  blockIdentifier: BlockBalanceTrace_BlockIdentifier | undefined;
}

export interface FreezeBalanceV2Contract {
  ownerAddress: Buffer;
  frozenBalance: Long;
  resource: ResourceCode;
}

export interface UnfreezeBalanceV2Contract {
  ownerAddress: Buffer;
  unfreezeBalance: Long;
  resource: ResourceCode;
}

export interface WithdrawExpireUnfreezeContract {
  ownerAddress: Buffer;
}

export interface DelegateResourceContract {
  ownerAddress: Buffer;
  resource: ResourceCode;
  balance: Long;
  receiverAddress: Buffer;
  lock: boolean;
  lockPeriod: Long;
}

export interface UnDelegateResourceContract {
  ownerAddress: Buffer;
  resource: ResourceCode;
  balance: Long;
  receiverAddress: Buffer;
}

export interface CancelAllUnfreezeV2Contract {
  ownerAddress: Buffer;
}

function createBaseFreezeBalanceContract(): FreezeBalanceContract {
  return {
    ownerAddress: Buffer.alloc(0),
    frozenBalance: Long.ZERO,
    frozenDuration: Long.ZERO,
    resource: 0,
    receiverAddress: Buffer.alloc(0),
  };
}

export const FreezeBalanceContract = {
  encode(message: FreezeBalanceContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (!message.frozenBalance.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.frozenBalance);
    }
    if (!message.frozenDuration.equals(Long.ZERO)) {
      writer.uint32(24).int64(message.frozenDuration);
    }
    if (message.resource !== 0) {
      writer.uint32(80).int32(message.resource);
    }
    if (message.receiverAddress.length !== 0) {
      writer.uint32(122).bytes(message.receiverAddress);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FreezeBalanceContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFreezeBalanceContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.frozenBalance = reader.int64() as Long;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.frozenDuration = reader.int64() as Long;
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.resource = reader.int32() as any;
          continue;
        case 15:
          if (tag !== 122) {
            break;
          }

          message.receiverAddress = reader.bytes() as Buffer;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FreezeBalanceContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      frozenBalance: isSet(object.frozenBalance) ? Long.fromValue(object.frozenBalance) : Long.ZERO,
      frozenDuration: isSet(object.frozenDuration) ? Long.fromValue(object.frozenDuration) : Long.ZERO,
      resource: isSet(object.resource) ? resourceCodeFromJSON(object.resource) : 0,
      receiverAddress: isSet(object.receiverAddress)
        ? Buffer.from(bytesFromBase64(object.receiverAddress))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: FreezeBalanceContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (!message.frozenBalance.equals(Long.ZERO)) {
      obj.frozenBalance = (message.frozenBalance || Long.ZERO).toString();
    }
    if (!message.frozenDuration.equals(Long.ZERO)) {
      obj.frozenDuration = (message.frozenDuration || Long.ZERO).toString();
    }
    if (message.resource !== 0) {
      obj.resource = resourceCodeToJSON(message.resource);
    }
    if (message.receiverAddress.length !== 0) {
      obj.receiverAddress = base64FromBytes(message.receiverAddress);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FreezeBalanceContract>, I>>(base?: I): FreezeBalanceContract {
    return FreezeBalanceContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FreezeBalanceContract>, I>>(object: I): FreezeBalanceContract {
    const message = createBaseFreezeBalanceContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.frozenBalance = (object.frozenBalance !== undefined && object.frozenBalance !== null)
      ? Long.fromValue(object.frozenBalance)
      : Long.ZERO;
    message.frozenDuration = (object.frozenDuration !== undefined && object.frozenDuration !== null)
      ? Long.fromValue(object.frozenDuration)
      : Long.ZERO;
    message.resource = object.resource ?? 0;
    message.receiverAddress = object.receiverAddress ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseUnfreezeBalanceContract(): UnfreezeBalanceContract {
  return { ownerAddress: Buffer.alloc(0), resource: 0, receiverAddress: Buffer.alloc(0) };
}

export const UnfreezeBalanceContract = {
  encode(message: UnfreezeBalanceContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (message.resource !== 0) {
      writer.uint32(80).int32(message.resource);
    }
    if (message.receiverAddress.length !== 0) {
      writer.uint32(122).bytes(message.receiverAddress);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UnfreezeBalanceContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnfreezeBalanceContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.resource = reader.int32() as any;
          continue;
        case 15:
          if (tag !== 122) {
            break;
          }

          message.receiverAddress = reader.bytes() as Buffer;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnfreezeBalanceContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      resource: isSet(object.resource) ? resourceCodeFromJSON(object.resource) : 0,
      receiverAddress: isSet(object.receiverAddress)
        ? Buffer.from(bytesFromBase64(object.receiverAddress))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: UnfreezeBalanceContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (message.resource !== 0) {
      obj.resource = resourceCodeToJSON(message.resource);
    }
    if (message.receiverAddress.length !== 0) {
      obj.receiverAddress = base64FromBytes(message.receiverAddress);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnfreezeBalanceContract>, I>>(base?: I): UnfreezeBalanceContract {
    return UnfreezeBalanceContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnfreezeBalanceContract>, I>>(object: I): UnfreezeBalanceContract {
    const message = createBaseUnfreezeBalanceContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.resource = object.resource ?? 0;
    message.receiverAddress = object.receiverAddress ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseWithdrawBalanceContract(): WithdrawBalanceContract {
  return { ownerAddress: Buffer.alloc(0) };
}

export const WithdrawBalanceContract = {
  encode(message: WithdrawBalanceContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): WithdrawBalanceContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWithdrawBalanceContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WithdrawBalanceContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
    };
  },

  toJSON(message: WithdrawBalanceContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WithdrawBalanceContract>, I>>(base?: I): WithdrawBalanceContract {
    return WithdrawBalanceContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WithdrawBalanceContract>, I>>(object: I): WithdrawBalanceContract {
    const message = createBaseWithdrawBalanceContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseTransferContract(): TransferContract {
  return { ownerAddress: Buffer.alloc(0), toAddress: Buffer.alloc(0), amount: Long.ZERO };
}

export const TransferContract = {
  encode(message: TransferContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (message.toAddress.length !== 0) {
      writer.uint32(18).bytes(message.toAddress);
    }
    if (!message.amount.equals(Long.ZERO)) {
      writer.uint32(24).int64(message.amount);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TransferContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransferContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.toAddress = reader.bytes() as Buffer;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.amount = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TransferContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      toAddress: isSet(object.toAddress) ? Buffer.from(bytesFromBase64(object.toAddress)) : Buffer.alloc(0),
      amount: isSet(object.amount) ? Long.fromValue(object.amount) : Long.ZERO,
    };
  },

  toJSON(message: TransferContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (message.toAddress.length !== 0) {
      obj.toAddress = base64FromBytes(message.toAddress);
    }
    if (!message.amount.equals(Long.ZERO)) {
      obj.amount = (message.amount || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TransferContract>, I>>(base?: I): TransferContract {
    return TransferContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TransferContract>, I>>(object: I): TransferContract {
    const message = createBaseTransferContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.toAddress = object.toAddress ?? Buffer.alloc(0);
    message.amount = (object.amount !== undefined && object.amount !== null)
      ? Long.fromValue(object.amount)
      : Long.ZERO;
    return message;
  },
};

function createBaseTransactionBalanceTrace(): TransactionBalanceTrace {
  return { transactionIdentifier: Buffer.alloc(0), operation: [], type: "", status: "" };
}

export const TransactionBalanceTrace = {
  encode(message: TransactionBalanceTrace, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.transactionIdentifier.length !== 0) {
      writer.uint32(10).bytes(message.transactionIdentifier);
    }
    for (const v of message.operation) {
      TransactionBalanceTrace_Operation.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.type !== "") {
      writer.uint32(26).string(message.type);
    }
    if (message.status !== "") {
      writer.uint32(34).string(message.status);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TransactionBalanceTrace {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransactionBalanceTrace();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.transactionIdentifier = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.operation.push(TransactionBalanceTrace_Operation.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.type = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.status = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TransactionBalanceTrace {
    return {
      transactionIdentifier: isSet(object.transactionIdentifier)
        ? Buffer.from(bytesFromBase64(object.transactionIdentifier))
        : Buffer.alloc(0),
      operation: globalThis.Array.isArray(object?.operation)
        ? object.operation.map((e: any) => TransactionBalanceTrace_Operation.fromJSON(e))
        : [],
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
    };
  },

  toJSON(message: TransactionBalanceTrace): unknown {
    const obj: any = {};
    if (message.transactionIdentifier.length !== 0) {
      obj.transactionIdentifier = base64FromBytes(message.transactionIdentifier);
    }
    if (message.operation?.length) {
      obj.operation = message.operation.map((e) => TransactionBalanceTrace_Operation.toJSON(e));
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TransactionBalanceTrace>, I>>(base?: I): TransactionBalanceTrace {
    return TransactionBalanceTrace.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TransactionBalanceTrace>, I>>(object: I): TransactionBalanceTrace {
    const message = createBaseTransactionBalanceTrace();
    message.transactionIdentifier = object.transactionIdentifier ?? Buffer.alloc(0);
    message.operation = object.operation?.map((e) => TransactionBalanceTrace_Operation.fromPartial(e)) || [];
    message.type = object.type ?? "";
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseTransactionBalanceTrace_Operation(): TransactionBalanceTrace_Operation {
  return { operationIdentifier: Long.ZERO, address: Buffer.alloc(0), amount: Long.ZERO };
}

export const TransactionBalanceTrace_Operation = {
  encode(message: TransactionBalanceTrace_Operation, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (!message.operationIdentifier.equals(Long.ZERO)) {
      writer.uint32(8).int64(message.operationIdentifier);
    }
    if (message.address.length !== 0) {
      writer.uint32(18).bytes(message.address);
    }
    if (!message.amount.equals(Long.ZERO)) {
      writer.uint32(24).int64(message.amount);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TransactionBalanceTrace_Operation {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransactionBalanceTrace_Operation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.operationIdentifier = reader.int64() as Long;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.address = reader.bytes() as Buffer;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.amount = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TransactionBalanceTrace_Operation {
    return {
      operationIdentifier: isSet(object.operationIdentifier) ? Long.fromValue(object.operationIdentifier) : Long.ZERO,
      address: isSet(object.address) ? Buffer.from(bytesFromBase64(object.address)) : Buffer.alloc(0),
      amount: isSet(object.amount) ? Long.fromValue(object.amount) : Long.ZERO,
    };
  },

  toJSON(message: TransactionBalanceTrace_Operation): unknown {
    const obj: any = {};
    if (!message.operationIdentifier.equals(Long.ZERO)) {
      obj.operationIdentifier = (message.operationIdentifier || Long.ZERO).toString();
    }
    if (message.address.length !== 0) {
      obj.address = base64FromBytes(message.address);
    }
    if (!message.amount.equals(Long.ZERO)) {
      obj.amount = (message.amount || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TransactionBalanceTrace_Operation>, I>>(
    base?: I,
  ): TransactionBalanceTrace_Operation {
    return TransactionBalanceTrace_Operation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TransactionBalanceTrace_Operation>, I>>(
    object: I,
  ): TransactionBalanceTrace_Operation {
    const message = createBaseTransactionBalanceTrace_Operation();
    message.operationIdentifier = (object.operationIdentifier !== undefined && object.operationIdentifier !== null)
      ? Long.fromValue(object.operationIdentifier)
      : Long.ZERO;
    message.address = object.address ?? Buffer.alloc(0);
    message.amount = (object.amount !== undefined && object.amount !== null)
      ? Long.fromValue(object.amount)
      : Long.ZERO;
    return message;
  },
};

function createBaseBlockBalanceTrace(): BlockBalanceTrace {
  return { blockIdentifier: undefined, timestamp: Long.ZERO, transactionBalanceTrace: [] };
}

export const BlockBalanceTrace = {
  encode(message: BlockBalanceTrace, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.blockIdentifier !== undefined) {
      BlockBalanceTrace_BlockIdentifier.encode(message.blockIdentifier, writer.uint32(10).fork()).ldelim();
    }
    if (!message.timestamp.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.timestamp);
    }
    for (const v of message.transactionBalanceTrace) {
      TransactionBalanceTrace.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BlockBalanceTrace {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBlockBalanceTrace();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.blockIdentifier = BlockBalanceTrace_BlockIdentifier.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.timestamp = reader.int64() as Long;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.transactionBalanceTrace.push(TransactionBalanceTrace.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BlockBalanceTrace {
    return {
      blockIdentifier: isSet(object.blockIdentifier)
        ? BlockBalanceTrace_BlockIdentifier.fromJSON(object.blockIdentifier)
        : undefined,
      timestamp: isSet(object.timestamp) ? Long.fromValue(object.timestamp) : Long.ZERO,
      transactionBalanceTrace: globalThis.Array.isArray(object?.transactionBalanceTrace)
        ? object.transactionBalanceTrace.map((e: any) => TransactionBalanceTrace.fromJSON(e))
        : [],
    };
  },

  toJSON(message: BlockBalanceTrace): unknown {
    const obj: any = {};
    if (message.blockIdentifier !== undefined) {
      obj.blockIdentifier = BlockBalanceTrace_BlockIdentifier.toJSON(message.blockIdentifier);
    }
    if (!message.timestamp.equals(Long.ZERO)) {
      obj.timestamp = (message.timestamp || Long.ZERO).toString();
    }
    if (message.transactionBalanceTrace?.length) {
      obj.transactionBalanceTrace = message.transactionBalanceTrace.map((e) => TransactionBalanceTrace.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BlockBalanceTrace>, I>>(base?: I): BlockBalanceTrace {
    return BlockBalanceTrace.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BlockBalanceTrace>, I>>(object: I): BlockBalanceTrace {
    const message = createBaseBlockBalanceTrace();
    message.blockIdentifier = (object.blockIdentifier !== undefined && object.blockIdentifier !== null)
      ? BlockBalanceTrace_BlockIdentifier.fromPartial(object.blockIdentifier)
      : undefined;
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Long.fromValue(object.timestamp)
      : Long.ZERO;
    message.transactionBalanceTrace =
      object.transactionBalanceTrace?.map((e) => TransactionBalanceTrace.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBlockBalanceTrace_BlockIdentifier(): BlockBalanceTrace_BlockIdentifier {
  return { hash: Buffer.alloc(0), number: Long.ZERO };
}

export const BlockBalanceTrace_BlockIdentifier = {
  encode(message: BlockBalanceTrace_BlockIdentifier, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.hash.length !== 0) {
      writer.uint32(10).bytes(message.hash);
    }
    if (!message.number.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.number);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BlockBalanceTrace_BlockIdentifier {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBlockBalanceTrace_BlockIdentifier();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.hash = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.number = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BlockBalanceTrace_BlockIdentifier {
    return {
      hash: isSet(object.hash) ? Buffer.from(bytesFromBase64(object.hash)) : Buffer.alloc(0),
      number: isSet(object.number) ? Long.fromValue(object.number) : Long.ZERO,
    };
  },

  toJSON(message: BlockBalanceTrace_BlockIdentifier): unknown {
    const obj: any = {};
    if (message.hash.length !== 0) {
      obj.hash = base64FromBytes(message.hash);
    }
    if (!message.number.equals(Long.ZERO)) {
      obj.number = (message.number || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BlockBalanceTrace_BlockIdentifier>, I>>(
    base?: I,
  ): BlockBalanceTrace_BlockIdentifier {
    return BlockBalanceTrace_BlockIdentifier.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BlockBalanceTrace_BlockIdentifier>, I>>(
    object: I,
  ): BlockBalanceTrace_BlockIdentifier {
    const message = createBaseBlockBalanceTrace_BlockIdentifier();
    message.hash = object.hash ?? Buffer.alloc(0);
    message.number = (object.number !== undefined && object.number !== null)
      ? Long.fromValue(object.number)
      : Long.ZERO;
    return message;
  },
};

function createBaseAccountTrace(): AccountTrace {
  return { balance: Long.ZERO, placeholder: Long.ZERO };
}

export const AccountTrace = {
  encode(message: AccountTrace, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (!message.balance.equals(Long.ZERO)) {
      writer.uint32(8).int64(message.balance);
    }
    if (!message.placeholder.equals(Long.ZERO)) {
      writer.uint32(792).int64(message.placeholder);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AccountTrace {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccountTrace();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.balance = reader.int64() as Long;
          continue;
        case 99:
          if (tag !== 792) {
            break;
          }

          message.placeholder = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AccountTrace {
    return {
      balance: isSet(object.balance) ? Long.fromValue(object.balance) : Long.ZERO,
      placeholder: isSet(object.placeholder) ? Long.fromValue(object.placeholder) : Long.ZERO,
    };
  },

  toJSON(message: AccountTrace): unknown {
    const obj: any = {};
    if (!message.balance.equals(Long.ZERO)) {
      obj.balance = (message.balance || Long.ZERO).toString();
    }
    if (!message.placeholder.equals(Long.ZERO)) {
      obj.placeholder = (message.placeholder || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AccountTrace>, I>>(base?: I): AccountTrace {
    return AccountTrace.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AccountTrace>, I>>(object: I): AccountTrace {
    const message = createBaseAccountTrace();
    message.balance = (object.balance !== undefined && object.balance !== null)
      ? Long.fromValue(object.balance)
      : Long.ZERO;
    message.placeholder = (object.placeholder !== undefined && object.placeholder !== null)
      ? Long.fromValue(object.placeholder)
      : Long.ZERO;
    return message;
  },
};

function createBaseAccountIdentifier(): AccountIdentifier {
  return { address: Buffer.alloc(0) };
}

export const AccountIdentifier = {
  encode(message: AccountIdentifier, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.address.length !== 0) {
      writer.uint32(10).bytes(message.address);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AccountIdentifier {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccountIdentifier();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.address = reader.bytes() as Buffer;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AccountIdentifier {
    return { address: isSet(object.address) ? Buffer.from(bytesFromBase64(object.address)) : Buffer.alloc(0) };
  },

  toJSON(message: AccountIdentifier): unknown {
    const obj: any = {};
    if (message.address.length !== 0) {
      obj.address = base64FromBytes(message.address);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AccountIdentifier>, I>>(base?: I): AccountIdentifier {
    return AccountIdentifier.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AccountIdentifier>, I>>(object: I): AccountIdentifier {
    const message = createBaseAccountIdentifier();
    message.address = object.address ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseAccountBalanceRequest(): AccountBalanceRequest {
  return { accountIdentifier: undefined, blockIdentifier: undefined };
}

export const AccountBalanceRequest = {
  encode(message: AccountBalanceRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.accountIdentifier !== undefined) {
      AccountIdentifier.encode(message.accountIdentifier, writer.uint32(10).fork()).ldelim();
    }
    if (message.blockIdentifier !== undefined) {
      BlockBalanceTrace_BlockIdentifier.encode(message.blockIdentifier, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AccountBalanceRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccountBalanceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.accountIdentifier = AccountIdentifier.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.blockIdentifier = BlockBalanceTrace_BlockIdentifier.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AccountBalanceRequest {
    return {
      accountIdentifier: isSet(object.accountIdentifier)
        ? AccountIdentifier.fromJSON(object.accountIdentifier)
        : undefined,
      blockIdentifier: isSet(object.blockIdentifier)
        ? BlockBalanceTrace_BlockIdentifier.fromJSON(object.blockIdentifier)
        : undefined,
    };
  },

  toJSON(message: AccountBalanceRequest): unknown {
    const obj: any = {};
    if (message.accountIdentifier !== undefined) {
      obj.accountIdentifier = AccountIdentifier.toJSON(message.accountIdentifier);
    }
    if (message.blockIdentifier !== undefined) {
      obj.blockIdentifier = BlockBalanceTrace_BlockIdentifier.toJSON(message.blockIdentifier);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AccountBalanceRequest>, I>>(base?: I): AccountBalanceRequest {
    return AccountBalanceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AccountBalanceRequest>, I>>(object: I): AccountBalanceRequest {
    const message = createBaseAccountBalanceRequest();
    message.accountIdentifier = (object.accountIdentifier !== undefined && object.accountIdentifier !== null)
      ? AccountIdentifier.fromPartial(object.accountIdentifier)
      : undefined;
    message.blockIdentifier = (object.blockIdentifier !== undefined && object.blockIdentifier !== null)
      ? BlockBalanceTrace_BlockIdentifier.fromPartial(object.blockIdentifier)
      : undefined;
    return message;
  },
};

function createBaseAccountBalanceResponse(): AccountBalanceResponse {
  return { balance: Long.ZERO, blockIdentifier: undefined };
}

export const AccountBalanceResponse = {
  encode(message: AccountBalanceResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (!message.balance.equals(Long.ZERO)) {
      writer.uint32(8).int64(message.balance);
    }
    if (message.blockIdentifier !== undefined) {
      BlockBalanceTrace_BlockIdentifier.encode(message.blockIdentifier, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AccountBalanceResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccountBalanceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.balance = reader.int64() as Long;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.blockIdentifier = BlockBalanceTrace_BlockIdentifier.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AccountBalanceResponse {
    return {
      balance: isSet(object.balance) ? Long.fromValue(object.balance) : Long.ZERO,
      blockIdentifier: isSet(object.blockIdentifier)
        ? BlockBalanceTrace_BlockIdentifier.fromJSON(object.blockIdentifier)
        : undefined,
    };
  },

  toJSON(message: AccountBalanceResponse): unknown {
    const obj: any = {};
    if (!message.balance.equals(Long.ZERO)) {
      obj.balance = (message.balance || Long.ZERO).toString();
    }
    if (message.blockIdentifier !== undefined) {
      obj.blockIdentifier = BlockBalanceTrace_BlockIdentifier.toJSON(message.blockIdentifier);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AccountBalanceResponse>, I>>(base?: I): AccountBalanceResponse {
    return AccountBalanceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AccountBalanceResponse>, I>>(object: I): AccountBalanceResponse {
    const message = createBaseAccountBalanceResponse();
    message.balance = (object.balance !== undefined && object.balance !== null)
      ? Long.fromValue(object.balance)
      : Long.ZERO;
    message.blockIdentifier = (object.blockIdentifier !== undefined && object.blockIdentifier !== null)
      ? BlockBalanceTrace_BlockIdentifier.fromPartial(object.blockIdentifier)
      : undefined;
    return message;
  },
};

function createBaseFreezeBalanceV2Contract(): FreezeBalanceV2Contract {
  return { ownerAddress: Buffer.alloc(0), frozenBalance: Long.ZERO, resource: 0 };
}

export const FreezeBalanceV2Contract = {
  encode(message: FreezeBalanceV2Contract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (!message.frozenBalance.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.frozenBalance);
    }
    if (message.resource !== 0) {
      writer.uint32(24).int32(message.resource);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FreezeBalanceV2Contract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFreezeBalanceV2Contract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.frozenBalance = reader.int64() as Long;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.resource = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FreezeBalanceV2Contract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      frozenBalance: isSet(object.frozenBalance) ? Long.fromValue(object.frozenBalance) : Long.ZERO,
      resource: isSet(object.resource) ? resourceCodeFromJSON(object.resource) : 0,
    };
  },

  toJSON(message: FreezeBalanceV2Contract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (!message.frozenBalance.equals(Long.ZERO)) {
      obj.frozenBalance = (message.frozenBalance || Long.ZERO).toString();
    }
    if (message.resource !== 0) {
      obj.resource = resourceCodeToJSON(message.resource);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FreezeBalanceV2Contract>, I>>(base?: I): FreezeBalanceV2Contract {
    return FreezeBalanceV2Contract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FreezeBalanceV2Contract>, I>>(object: I): FreezeBalanceV2Contract {
    const message = createBaseFreezeBalanceV2Contract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.frozenBalance = (object.frozenBalance !== undefined && object.frozenBalance !== null)
      ? Long.fromValue(object.frozenBalance)
      : Long.ZERO;
    message.resource = object.resource ?? 0;
    return message;
  },
};

function createBaseUnfreezeBalanceV2Contract(): UnfreezeBalanceV2Contract {
  return { ownerAddress: Buffer.alloc(0), unfreezeBalance: Long.ZERO, resource: 0 };
}

export const UnfreezeBalanceV2Contract = {
  encode(message: UnfreezeBalanceV2Contract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (!message.unfreezeBalance.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.unfreezeBalance);
    }
    if (message.resource !== 0) {
      writer.uint32(24).int32(message.resource);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UnfreezeBalanceV2Contract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnfreezeBalanceV2Contract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.unfreezeBalance = reader.int64() as Long;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.resource = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnfreezeBalanceV2Contract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      unfreezeBalance: isSet(object.unfreezeBalance) ? Long.fromValue(object.unfreezeBalance) : Long.ZERO,
      resource: isSet(object.resource) ? resourceCodeFromJSON(object.resource) : 0,
    };
  },

  toJSON(message: UnfreezeBalanceV2Contract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (!message.unfreezeBalance.equals(Long.ZERO)) {
      obj.unfreezeBalance = (message.unfreezeBalance || Long.ZERO).toString();
    }
    if (message.resource !== 0) {
      obj.resource = resourceCodeToJSON(message.resource);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnfreezeBalanceV2Contract>, I>>(base?: I): UnfreezeBalanceV2Contract {
    return UnfreezeBalanceV2Contract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnfreezeBalanceV2Contract>, I>>(object: I): UnfreezeBalanceV2Contract {
    const message = createBaseUnfreezeBalanceV2Contract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.unfreezeBalance = (object.unfreezeBalance !== undefined && object.unfreezeBalance !== null)
      ? Long.fromValue(object.unfreezeBalance)
      : Long.ZERO;
    message.resource = object.resource ?? 0;
    return message;
  },
};

function createBaseWithdrawExpireUnfreezeContract(): WithdrawExpireUnfreezeContract {
  return { ownerAddress: Buffer.alloc(0) };
}

export const WithdrawExpireUnfreezeContract = {
  encode(message: WithdrawExpireUnfreezeContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): WithdrawExpireUnfreezeContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWithdrawExpireUnfreezeContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WithdrawExpireUnfreezeContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
    };
  },

  toJSON(message: WithdrawExpireUnfreezeContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WithdrawExpireUnfreezeContract>, I>>(base?: I): WithdrawExpireUnfreezeContract {
    return WithdrawExpireUnfreezeContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WithdrawExpireUnfreezeContract>, I>>(
    object: I,
  ): WithdrawExpireUnfreezeContract {
    const message = createBaseWithdrawExpireUnfreezeContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseDelegateResourceContract(): DelegateResourceContract {
  return {
    ownerAddress: Buffer.alloc(0),
    resource: 0,
    balance: Long.ZERO,
    receiverAddress: Buffer.alloc(0),
    lock: false,
    lockPeriod: Long.ZERO,
  };
}

export const DelegateResourceContract = {
  encode(message: DelegateResourceContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (message.resource !== 0) {
      writer.uint32(16).int32(message.resource);
    }
    if (!message.balance.equals(Long.ZERO)) {
      writer.uint32(24).int64(message.balance);
    }
    if (message.receiverAddress.length !== 0) {
      writer.uint32(34).bytes(message.receiverAddress);
    }
    if (message.lock !== false) {
      writer.uint32(40).bool(message.lock);
    }
    if (!message.lockPeriod.equals(Long.ZERO)) {
      writer.uint32(48).int64(message.lockPeriod);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DelegateResourceContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDelegateResourceContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.resource = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.balance = reader.int64() as Long;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.receiverAddress = reader.bytes() as Buffer;
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.lock = reader.bool();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.lockPeriod = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DelegateResourceContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      resource: isSet(object.resource) ? resourceCodeFromJSON(object.resource) : 0,
      balance: isSet(object.balance) ? Long.fromValue(object.balance) : Long.ZERO,
      receiverAddress: isSet(object.receiverAddress)
        ? Buffer.from(bytesFromBase64(object.receiverAddress))
        : Buffer.alloc(0),
      lock: isSet(object.lock) ? globalThis.Boolean(object.lock) : false,
      lockPeriod: isSet(object.lockPeriod) ? Long.fromValue(object.lockPeriod) : Long.ZERO,
    };
  },

  toJSON(message: DelegateResourceContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (message.resource !== 0) {
      obj.resource = resourceCodeToJSON(message.resource);
    }
    if (!message.balance.equals(Long.ZERO)) {
      obj.balance = (message.balance || Long.ZERO).toString();
    }
    if (message.receiverAddress.length !== 0) {
      obj.receiverAddress = base64FromBytes(message.receiverAddress);
    }
    if (message.lock !== false) {
      obj.lock = message.lock;
    }
    if (!message.lockPeriod.equals(Long.ZERO)) {
      obj.lockPeriod = (message.lockPeriod || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DelegateResourceContract>, I>>(base?: I): DelegateResourceContract {
    return DelegateResourceContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DelegateResourceContract>, I>>(object: I): DelegateResourceContract {
    const message = createBaseDelegateResourceContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.resource = object.resource ?? 0;
    message.balance = (object.balance !== undefined && object.balance !== null)
      ? Long.fromValue(object.balance)
      : Long.ZERO;
    message.receiverAddress = object.receiverAddress ?? Buffer.alloc(0);
    message.lock = object.lock ?? false;
    message.lockPeriod = (object.lockPeriod !== undefined && object.lockPeriod !== null)
      ? Long.fromValue(object.lockPeriod)
      : Long.ZERO;
    return message;
  },
};

function createBaseUnDelegateResourceContract(): UnDelegateResourceContract {
  return { ownerAddress: Buffer.alloc(0), resource: 0, balance: Long.ZERO, receiverAddress: Buffer.alloc(0) };
}

export const UnDelegateResourceContract = {
  encode(message: UnDelegateResourceContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (message.resource !== 0) {
      writer.uint32(16).int32(message.resource);
    }
    if (!message.balance.equals(Long.ZERO)) {
      writer.uint32(24).int64(message.balance);
    }
    if (message.receiverAddress.length !== 0) {
      writer.uint32(34).bytes(message.receiverAddress);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UnDelegateResourceContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnDelegateResourceContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.resource = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.balance = reader.int64() as Long;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.receiverAddress = reader.bytes() as Buffer;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnDelegateResourceContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      resource: isSet(object.resource) ? resourceCodeFromJSON(object.resource) : 0,
      balance: isSet(object.balance) ? Long.fromValue(object.balance) : Long.ZERO,
      receiverAddress: isSet(object.receiverAddress)
        ? Buffer.from(bytesFromBase64(object.receiverAddress))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: UnDelegateResourceContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (message.resource !== 0) {
      obj.resource = resourceCodeToJSON(message.resource);
    }
    if (!message.balance.equals(Long.ZERO)) {
      obj.balance = (message.balance || Long.ZERO).toString();
    }
    if (message.receiverAddress.length !== 0) {
      obj.receiverAddress = base64FromBytes(message.receiverAddress);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnDelegateResourceContract>, I>>(base?: I): UnDelegateResourceContract {
    return UnDelegateResourceContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnDelegateResourceContract>, I>>(object: I): UnDelegateResourceContract {
    const message = createBaseUnDelegateResourceContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.resource = object.resource ?? 0;
    message.balance = (object.balance !== undefined && object.balance !== null)
      ? Long.fromValue(object.balance)
      : Long.ZERO;
    message.receiverAddress = object.receiverAddress ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseCancelAllUnfreezeV2Contract(): CancelAllUnfreezeV2Contract {
  return { ownerAddress: Buffer.alloc(0) };
}

export const CancelAllUnfreezeV2Contract = {
  encode(message: CancelAllUnfreezeV2Contract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CancelAllUnfreezeV2Contract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelAllUnfreezeV2Contract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CancelAllUnfreezeV2Contract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
    };
  },

  toJSON(message: CancelAllUnfreezeV2Contract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CancelAllUnfreezeV2Contract>, I>>(base?: I): CancelAllUnfreezeV2Contract {
    return CancelAllUnfreezeV2Contract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CancelAllUnfreezeV2Contract>, I>>(object: I): CancelAllUnfreezeV2Contract {
    const message = createBaseCancelAllUnfreezeV2Contract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
