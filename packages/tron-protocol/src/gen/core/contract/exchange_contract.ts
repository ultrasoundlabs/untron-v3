// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v5.29.3
// source: core/contract/exchange_contract.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "protocol";

export interface ExchangeCreateContract {
  ownerAddress: Buffer;
  firstTokenId: Buffer;
  firstTokenBalance: Long;
  secondTokenId: Buffer;
  secondTokenBalance: Long;
}

export interface ExchangeInjectContract {
  ownerAddress: Buffer;
  exchangeId: Long;
  tokenId: Buffer;
  quant: Long;
}

export interface ExchangeWithdrawContract {
  ownerAddress: Buffer;
  exchangeId: Long;
  tokenId: Buffer;
  quant: Long;
}

export interface ExchangeTransactionContract {
  ownerAddress: Buffer;
  exchangeId: Long;
  tokenId: Buffer;
  quant: Long;
  expected: Long;
}

function createBaseExchangeCreateContract(): ExchangeCreateContract {
  return {
    ownerAddress: Buffer.alloc(0),
    firstTokenId: Buffer.alloc(0),
    firstTokenBalance: Long.ZERO,
    secondTokenId: Buffer.alloc(0),
    secondTokenBalance: Long.ZERO,
  };
}

export const ExchangeCreateContract = {
  encode(message: ExchangeCreateContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (message.firstTokenId.length !== 0) {
      writer.uint32(18).bytes(message.firstTokenId);
    }
    if (!message.firstTokenBalance.equals(Long.ZERO)) {
      writer.uint32(24).int64(message.firstTokenBalance);
    }
    if (message.secondTokenId.length !== 0) {
      writer.uint32(34).bytes(message.secondTokenId);
    }
    if (!message.secondTokenBalance.equals(Long.ZERO)) {
      writer.uint32(40).int64(message.secondTokenBalance);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ExchangeCreateContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExchangeCreateContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.firstTokenId = reader.bytes() as Buffer;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.firstTokenBalance = reader.int64() as Long;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.secondTokenId = reader.bytes() as Buffer;
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.secondTokenBalance = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExchangeCreateContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      firstTokenId: isSet(object.firstTokenId) ? Buffer.from(bytesFromBase64(object.firstTokenId)) : Buffer.alloc(0),
      firstTokenBalance: isSet(object.firstTokenBalance) ? Long.fromValue(object.firstTokenBalance) : Long.ZERO,
      secondTokenId: isSet(object.secondTokenId) ? Buffer.from(bytesFromBase64(object.secondTokenId)) : Buffer.alloc(0),
      secondTokenBalance: isSet(object.secondTokenBalance) ? Long.fromValue(object.secondTokenBalance) : Long.ZERO,
    };
  },

  toJSON(message: ExchangeCreateContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (message.firstTokenId.length !== 0) {
      obj.firstTokenId = base64FromBytes(message.firstTokenId);
    }
    if (!message.firstTokenBalance.equals(Long.ZERO)) {
      obj.firstTokenBalance = (message.firstTokenBalance || Long.ZERO).toString();
    }
    if (message.secondTokenId.length !== 0) {
      obj.secondTokenId = base64FromBytes(message.secondTokenId);
    }
    if (!message.secondTokenBalance.equals(Long.ZERO)) {
      obj.secondTokenBalance = (message.secondTokenBalance || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExchangeCreateContract>, I>>(base?: I): ExchangeCreateContract {
    return ExchangeCreateContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExchangeCreateContract>, I>>(object: I): ExchangeCreateContract {
    const message = createBaseExchangeCreateContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.firstTokenId = object.firstTokenId ?? Buffer.alloc(0);
    message.firstTokenBalance = (object.firstTokenBalance !== undefined && object.firstTokenBalance !== null)
      ? Long.fromValue(object.firstTokenBalance)
      : Long.ZERO;
    message.secondTokenId = object.secondTokenId ?? Buffer.alloc(0);
    message.secondTokenBalance = (object.secondTokenBalance !== undefined && object.secondTokenBalance !== null)
      ? Long.fromValue(object.secondTokenBalance)
      : Long.ZERO;
    return message;
  },
};

function createBaseExchangeInjectContract(): ExchangeInjectContract {
  return { ownerAddress: Buffer.alloc(0), exchangeId: Long.ZERO, tokenId: Buffer.alloc(0), quant: Long.ZERO };
}

export const ExchangeInjectContract = {
  encode(message: ExchangeInjectContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (!message.exchangeId.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.exchangeId);
    }
    if (message.tokenId.length !== 0) {
      writer.uint32(26).bytes(message.tokenId);
    }
    if (!message.quant.equals(Long.ZERO)) {
      writer.uint32(32).int64(message.quant);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ExchangeInjectContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExchangeInjectContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.exchangeId = reader.int64() as Long;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.tokenId = reader.bytes() as Buffer;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.quant = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExchangeInjectContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      exchangeId: isSet(object.exchangeId) ? Long.fromValue(object.exchangeId) : Long.ZERO,
      tokenId: isSet(object.tokenId) ? Buffer.from(bytesFromBase64(object.tokenId)) : Buffer.alloc(0),
      quant: isSet(object.quant) ? Long.fromValue(object.quant) : Long.ZERO,
    };
  },

  toJSON(message: ExchangeInjectContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (!message.exchangeId.equals(Long.ZERO)) {
      obj.exchangeId = (message.exchangeId || Long.ZERO).toString();
    }
    if (message.tokenId.length !== 0) {
      obj.tokenId = base64FromBytes(message.tokenId);
    }
    if (!message.quant.equals(Long.ZERO)) {
      obj.quant = (message.quant || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExchangeInjectContract>, I>>(base?: I): ExchangeInjectContract {
    return ExchangeInjectContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExchangeInjectContract>, I>>(object: I): ExchangeInjectContract {
    const message = createBaseExchangeInjectContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.exchangeId = (object.exchangeId !== undefined && object.exchangeId !== null)
      ? Long.fromValue(object.exchangeId)
      : Long.ZERO;
    message.tokenId = object.tokenId ?? Buffer.alloc(0);
    message.quant = (object.quant !== undefined && object.quant !== null) ? Long.fromValue(object.quant) : Long.ZERO;
    return message;
  },
};

function createBaseExchangeWithdrawContract(): ExchangeWithdrawContract {
  return { ownerAddress: Buffer.alloc(0), exchangeId: Long.ZERO, tokenId: Buffer.alloc(0), quant: Long.ZERO };
}

export const ExchangeWithdrawContract = {
  encode(message: ExchangeWithdrawContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (!message.exchangeId.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.exchangeId);
    }
    if (message.tokenId.length !== 0) {
      writer.uint32(26).bytes(message.tokenId);
    }
    if (!message.quant.equals(Long.ZERO)) {
      writer.uint32(32).int64(message.quant);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ExchangeWithdrawContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExchangeWithdrawContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.exchangeId = reader.int64() as Long;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.tokenId = reader.bytes() as Buffer;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.quant = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExchangeWithdrawContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      exchangeId: isSet(object.exchangeId) ? Long.fromValue(object.exchangeId) : Long.ZERO,
      tokenId: isSet(object.tokenId) ? Buffer.from(bytesFromBase64(object.tokenId)) : Buffer.alloc(0),
      quant: isSet(object.quant) ? Long.fromValue(object.quant) : Long.ZERO,
    };
  },

  toJSON(message: ExchangeWithdrawContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (!message.exchangeId.equals(Long.ZERO)) {
      obj.exchangeId = (message.exchangeId || Long.ZERO).toString();
    }
    if (message.tokenId.length !== 0) {
      obj.tokenId = base64FromBytes(message.tokenId);
    }
    if (!message.quant.equals(Long.ZERO)) {
      obj.quant = (message.quant || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExchangeWithdrawContract>, I>>(base?: I): ExchangeWithdrawContract {
    return ExchangeWithdrawContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExchangeWithdrawContract>, I>>(object: I): ExchangeWithdrawContract {
    const message = createBaseExchangeWithdrawContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.exchangeId = (object.exchangeId !== undefined && object.exchangeId !== null)
      ? Long.fromValue(object.exchangeId)
      : Long.ZERO;
    message.tokenId = object.tokenId ?? Buffer.alloc(0);
    message.quant = (object.quant !== undefined && object.quant !== null) ? Long.fromValue(object.quant) : Long.ZERO;
    return message;
  },
};

function createBaseExchangeTransactionContract(): ExchangeTransactionContract {
  return {
    ownerAddress: Buffer.alloc(0),
    exchangeId: Long.ZERO,
    tokenId: Buffer.alloc(0),
    quant: Long.ZERO,
    expected: Long.ZERO,
  };
}

export const ExchangeTransactionContract = {
  encode(message: ExchangeTransactionContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (!message.exchangeId.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.exchangeId);
    }
    if (message.tokenId.length !== 0) {
      writer.uint32(26).bytes(message.tokenId);
    }
    if (!message.quant.equals(Long.ZERO)) {
      writer.uint32(32).int64(message.quant);
    }
    if (!message.expected.equals(Long.ZERO)) {
      writer.uint32(40).int64(message.expected);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ExchangeTransactionContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExchangeTransactionContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.exchangeId = reader.int64() as Long;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.tokenId = reader.bytes() as Buffer;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.quant = reader.int64() as Long;
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.expected = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExchangeTransactionContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      exchangeId: isSet(object.exchangeId) ? Long.fromValue(object.exchangeId) : Long.ZERO,
      tokenId: isSet(object.tokenId) ? Buffer.from(bytesFromBase64(object.tokenId)) : Buffer.alloc(0),
      quant: isSet(object.quant) ? Long.fromValue(object.quant) : Long.ZERO,
      expected: isSet(object.expected) ? Long.fromValue(object.expected) : Long.ZERO,
    };
  },

  toJSON(message: ExchangeTransactionContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (!message.exchangeId.equals(Long.ZERO)) {
      obj.exchangeId = (message.exchangeId || Long.ZERO).toString();
    }
    if (message.tokenId.length !== 0) {
      obj.tokenId = base64FromBytes(message.tokenId);
    }
    if (!message.quant.equals(Long.ZERO)) {
      obj.quant = (message.quant || Long.ZERO).toString();
    }
    if (!message.expected.equals(Long.ZERO)) {
      obj.expected = (message.expected || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExchangeTransactionContract>, I>>(base?: I): ExchangeTransactionContract {
    return ExchangeTransactionContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExchangeTransactionContract>, I>>(object: I): ExchangeTransactionContract {
    const message = createBaseExchangeTransactionContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.exchangeId = (object.exchangeId !== undefined && object.exchangeId !== null)
      ? Long.fromValue(object.exchangeId)
      : Long.ZERO;
    message.tokenId = object.tokenId ?? Buffer.alloc(0);
    message.quant = (object.quant !== undefined && object.quant !== null) ? Long.fromValue(object.quant) : Long.ZERO;
    message.expected = (object.expected !== undefined && object.expected !== null)
      ? Long.fromValue(object.expected)
      : Long.ZERO;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
