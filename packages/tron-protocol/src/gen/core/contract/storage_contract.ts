// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v5.29.3
// source: core/contract/storage_contract.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "protocol";

export interface BuyStorageBytesContract {
  ownerAddress: Buffer;
  /** storage bytes for buy */
  bytes: Long;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

export interface BuyStorageContract {
  ownerAddress: Buffer;
  /** trx quantity for buy storage (sun) */
  quant: Long;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

export interface SellStorageContract {
  ownerAddress: Buffer;
  storageBytes: Long;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

export interface UpdateBrokerageContract {
  ownerAddress: Buffer;
  /** 1 mean 1% */
  brokerage: number;
  _unknownFields?: { [key: number]: Uint8Array[] } | undefined;
}

function createBaseBuyStorageBytesContract(): BuyStorageBytesContract {
  return { ownerAddress: Buffer.alloc(0), bytes: Long.ZERO, _unknownFields: {} };
}

export const BuyStorageBytesContract = {
  encode(message: BuyStorageBytesContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (!message.bytes.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.bytes);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag);
          (writer as any)["_push"](
            (val: Uint8Array, buf: Buffer, pos: number) => buf.set(val, pos),
            value.length,
            value,
          );
        }
      }
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BuyStorageBytesContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuyStorageBytesContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.bytes = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const startPos = reader.pos;
      reader.skipType(tag & 7);
      const buf = reader.buf.slice(startPos, reader.pos);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): BuyStorageBytesContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      bytes: isSet(object.bytes) ? Long.fromValue(object.bytes) : Long.ZERO,
    };
  },

  toJSON(message: BuyStorageBytesContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (!message.bytes.equals(Long.ZERO)) {
      obj.bytes = (message.bytes || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuyStorageBytesContract>, I>>(base?: I): BuyStorageBytesContract {
    return BuyStorageBytesContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuyStorageBytesContract>, I>>(object: I): BuyStorageBytesContract {
    const message = createBaseBuyStorageBytesContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.bytes = (object.bytes !== undefined && object.bytes !== null) ? Long.fromValue(object.bytes) : Long.ZERO;
    return message;
  },
};

function createBaseBuyStorageContract(): BuyStorageContract {
  return { ownerAddress: Buffer.alloc(0), quant: Long.ZERO, _unknownFields: {} };
}

export const BuyStorageContract = {
  encode(message: BuyStorageContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (!message.quant.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.quant);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag);
          (writer as any)["_push"](
            (val: Uint8Array, buf: Buffer, pos: number) => buf.set(val, pos),
            value.length,
            value,
          );
        }
      }
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BuyStorageContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuyStorageContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.quant = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const startPos = reader.pos;
      reader.skipType(tag & 7);
      const buf = reader.buf.slice(startPos, reader.pos);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): BuyStorageContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      quant: isSet(object.quant) ? Long.fromValue(object.quant) : Long.ZERO,
    };
  },

  toJSON(message: BuyStorageContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (!message.quant.equals(Long.ZERO)) {
      obj.quant = (message.quant || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuyStorageContract>, I>>(base?: I): BuyStorageContract {
    return BuyStorageContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuyStorageContract>, I>>(object: I): BuyStorageContract {
    const message = createBaseBuyStorageContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.quant = (object.quant !== undefined && object.quant !== null) ? Long.fromValue(object.quant) : Long.ZERO;
    return message;
  },
};

function createBaseSellStorageContract(): SellStorageContract {
  return { ownerAddress: Buffer.alloc(0), storageBytes: Long.ZERO, _unknownFields: {} };
}

export const SellStorageContract = {
  encode(message: SellStorageContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (!message.storageBytes.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.storageBytes);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag);
          (writer as any)["_push"](
            (val: Uint8Array, buf: Buffer, pos: number) => buf.set(val, pos),
            value.length,
            value,
          );
        }
      }
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SellStorageContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSellStorageContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.storageBytes = reader.int64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const startPos = reader.pos;
      reader.skipType(tag & 7);
      const buf = reader.buf.slice(startPos, reader.pos);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): SellStorageContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      storageBytes: isSet(object.storageBytes) ? Long.fromValue(object.storageBytes) : Long.ZERO,
    };
  },

  toJSON(message: SellStorageContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (!message.storageBytes.equals(Long.ZERO)) {
      obj.storageBytes = (message.storageBytes || Long.ZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SellStorageContract>, I>>(base?: I): SellStorageContract {
    return SellStorageContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SellStorageContract>, I>>(object: I): SellStorageContract {
    const message = createBaseSellStorageContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.storageBytes = (object.storageBytes !== undefined && object.storageBytes !== null)
      ? Long.fromValue(object.storageBytes)
      : Long.ZERO;
    return message;
  },
};

function createBaseUpdateBrokerageContract(): UpdateBrokerageContract {
  return { ownerAddress: Buffer.alloc(0), brokerage: 0, _unknownFields: {} };
}

export const UpdateBrokerageContract = {
  encode(message: UpdateBrokerageContract, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ownerAddress.length !== 0) {
      writer.uint32(10).bytes(message.ownerAddress);
    }
    if (message.brokerage !== 0) {
      writer.uint32(16).int32(message.brokerage);
    }
    if (message._unknownFields !== undefined) {
      for (const [key, values] of Object.entries(message._unknownFields)) {
        const tag = parseInt(key, 10);
        for (const value of values) {
          writer.uint32(tag);
          (writer as any)["_push"](
            (val: Uint8Array, buf: Buffer, pos: number) => buf.set(val, pos),
            value.length,
            value,
          );
        }
      }
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UpdateBrokerageContract {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateBrokerageContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ownerAddress = reader.bytes() as Buffer;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.brokerage = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      const startPos = reader.pos;
      reader.skipType(tag & 7);
      const buf = reader.buf.slice(startPos, reader.pos);

      const list = message._unknownFields![tag];

      if (list === undefined) {
        message._unknownFields![tag] = [buf];
      } else {
        list.push(buf);
      }
    }
    return message;
  },

  fromJSON(object: any): UpdateBrokerageContract {
    return {
      ownerAddress: isSet(object.ownerAddress) ? Buffer.from(bytesFromBase64(object.ownerAddress)) : Buffer.alloc(0),
      brokerage: isSet(object.brokerage) ? globalThis.Number(object.brokerage) : 0,
    };
  },

  toJSON(message: UpdateBrokerageContract): unknown {
    const obj: any = {};
    if (message.ownerAddress.length !== 0) {
      obj.ownerAddress = base64FromBytes(message.ownerAddress);
    }
    if (message.brokerage !== 0) {
      obj.brokerage = Math.round(message.brokerage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateBrokerageContract>, I>>(base?: I): UpdateBrokerageContract {
    return UpdateBrokerageContract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateBrokerageContract>, I>>(object: I): UpdateBrokerageContract {
    const message = createBaseUpdateBrokerageContract();
    message.ownerAddress = object.ownerAddress ?? Buffer.alloc(0);
    message.brokerage = object.brokerage ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
